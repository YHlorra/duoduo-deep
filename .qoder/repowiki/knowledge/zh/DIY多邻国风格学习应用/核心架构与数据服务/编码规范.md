- 数据模型类统一实现 `toMap()` 和 `fromMap()` 方法以支持 SQLite 序列化，并提供 `copyWith()` 方法用于不可变对象的属性更新。
- UI 组件广泛采用 16px 的圆角半径（`BorderRadius.circular(16)`）和粗体字重（`FontWeight.w800`）以维持多邻国风格的视觉一致性。
- 服务类（如 `OpenAIService`, `GamificationService`）通过构造函数注入依赖或在 Provider 中初始化，避免在业务逻辑中直接实例化底层工具类。