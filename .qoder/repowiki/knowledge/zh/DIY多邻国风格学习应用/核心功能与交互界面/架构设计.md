- 采用按业务域划分的扁平化目录结构（home, deck, learning, ingestion, profile, settings），各域内包含独立的 Screen 组件。
- 遵循“页面-逻辑-组件”分层：Screen 负责布局与状态监听，通过 Riverpod (ref.watch/ref.read) 消费全局 Providers，复杂交互逻辑下沉至 widgets/ 子目录（如 question_widgets.dart）。
- 模块间通过 Navigator 进行显式路由跳转，形成以 Home 为入口、Ingestion 为内容输入、Learning 为核心交互的线性工作流。
- 依赖方向明确指向 core/providers 和 data/models，确保 UI 层与底层数据及服务解耦。